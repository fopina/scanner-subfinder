name: test

on:
  pull_request:
    branches:
      - main

jobs:
  build-test:
    # if: startsWith(github.head_ref, 'dev') == false
    uses: fopina/scanner-go-entrypoint/.github/workflows/build-n-push.yml@support_test
    with:
      push: false
      tags: x
